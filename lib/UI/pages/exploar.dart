import 'package:flutter/material.dart';

class Exploar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ExploarPages"),
      ),
      body: Center(
        child: Container(
          child: Text("ExploarPages"),
        ),
      ),
    );
  }
}
